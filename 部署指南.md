# AIMS 部署指南

> 更新日期：2026-04-28
> 适用环境：Windows本地开发、Docker Compose测试、K8s生产

---

## 部署环境概览

AIMS支持三种部署环境：

| 环境 | 用途 | 部署方式 | 适用场景 |
|------|------|----------|----------|
| **本地开发环境** | 代码开发、功能测试 | Windows + Docker Desktop | 开发人员日常开发 |
| **测试环境** | 集成测试、功能验证 | Docker Compose | 测试团队功能验证 |
| **生产环境** | 生产运行、高可用 | Kubernetes + Helm | 生产环境部署 |

---

## 一、本地开发环境部署

### 环境要求

- **Docker Desktop**：4.0+ 版本
- **Git**：最新版本
- **Node.js**：18+ 版本
- **Python**：3.10+ 版本（可选）
- **内存**：8GB+（建议16GB）
- **磁盘**：D盘至少50GB可用空间

### 快速启动

```bash
# 1. 启动Docker中间件
docker compose up -d

# 2. 启动OpenClaw服务
D:\Project\aims\start-aims.bat
# 选择选项1：启动OpenClaw服务（开发模式）
```

### 验证环境

```bash
# 环境验证
powershell -ExecutionPolicy Bypass -File "D:\Project\aims\test-openclaw.ps1"

# Gateway健康检查
Invoke-WebRequest http://localhost:18789/health -UseBasicParsing
```

### 详细文档

详细配置和故障排查请参考：[本地开发环境部署指南.md](./本地开发环境部署指南.md)

---

## 二、Docker Compose测试环境部署

### 环境要求

- **Docker**：20.10+
- **Docker Compose**：2.0+
- **内存**：8GB+
- **磁盘**：至少50GB

### 部署步骤

#### 1. 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑配置
nano .env
```

关键配置项：

```env
# 服务端口
AIMS_GATEWAY_PORT=18789
AIMS_MYSQL_HOST_PORT=23306
AIMS_REDIS_HOST_PORT=26379
AIMS_MILVUS_GRPC_PORT=19530
AIMS_QDRANT_HTTP_PORT=16333

# LLM API密钥
DEEPSEEK_API_KEY=your-deepseek-api-key
MOONSHOT_API_KEY=your-moonshot-api-key
ZHIPU_API_KEY=your-zhipu-api-key

# 数据库密码
MYSQL_ROOT_PASSWORD=aims@2026
MYSQL_PASSWORD=aims@2026
MINIO_ROOT_PASSWORD=aims@2026
```

#### 2. 启动服务

```bash
# 启动所有服务
docker compose up -d

# 查看服务状态
docker compose ps

# 查看日志
docker compose logs -f
```

#### 3. 验证部署

```bash
# 检查所有服务状态
docker compose ps

# 检查OpenClaw Gateway
curl http://localhost:18789/health

# 检查MySQL
docker compose exec mysql mysql -u aims -paims@2026 -e "SHOW DATABASES;"

# 检查Redis
docker compose exec redis redis-cli ping

# 检查Milvus
curl http://localhost:19091/healthz

# 检查Qdrant
curl http://localhost:16333/health
```

#### 4. 服务访问

| 服务 | 访问地址 | 用途 |
|------|----------|------|
| OpenClaw Gateway | http://localhost:18789 | 主服务入口 |
| MinIO Console | http://localhost:19001 | 对象存储管理 |
| Milvus Dashboard | http://localhost:19091 | 向量数据库监控 |
| Qdrant Dashboard | http://localhost:16333 | 向量数据库管理 |

### 常用操作

```bash
# 停止服务
docker compose down

# 停止并删除数据
docker compose down -v

# 重启服务
docker compose restart

# 查看特定服务日志
docker compose logs -f openclaw
docker compose logs -f mysql
docker compose logs -f milvus

# 进入容器
docker compose exec openclaw sh
docker compose exec mysql bash
```

### 数据持久化

所有数据存储在 `D:\Project\aims\data\` 目录：

```
D:\Project\aims\data\
├── mysql-data/      # MySQL数据
├── redis-data/      # Redis数据
├── milvus-data/     # Milvus向量数据
├── qdrant-data/     # Qdrant向量数据
└── minio-data/      # MinIO对象存储
```

### 备份和恢复

```bash
# 备份MySQL
docker compose exec mysql mysqldump -u root -paims@2026 aims > backup.sql

# 恢复MySQL
docker compose exec -T mysql mysql -u root -paims@2026 aims < backup.sql

# 备份Milvus数据
docker cp aims-milvus:/var/lib/milvus ./milvus-backup

# 备份Qdrant数据
docker cp aims-qdrant:/qdrant/storage ./qdrant-backup
```

---

## 三、Kubernetes生产环境部署

### 环境要求

- **Kubernetes**：1.20+
- **Helm**：3.0+
- **存储类**：配置好的StorageClass
- **Ingress Controller**：推荐nginx
- **资源**：CPU 8核+，内存 16GB+

### 部署方式

#### 方式1：使用kubectl直接部署

```bash
# 1. 创建命名空间
kubectl apply -f k8s/namespace.yaml

# 2. 部署配置
kubectl apply -f k8s/configmap.yaml
kubectl apply -f k8s/secret.yaml
kubectl apply -f k8s/configmaps.yaml

# 3. 部署存储
kubectl apply -f k8s/pvc.yaml

# 4. 部署服务
kubectl apply -f k8s/deployments.yaml
kubectl apply -f k8s/services.yaml

# 5. 部署网络和入口
kubectl apply -f k8s/network-policy.yaml
kubectl apply -f k8s/ingress.yaml
```

#### 方式2：使用Helm部署（推荐）

```bash
# 1. 安装Helm Chart
helm install aims helm/aims -n aims --create-namespace

# 2. 自定义配置安装
helm install aims helm/aims -n aims --create-namespace \
  --set openclaw.replicaCount=3 \
  --set mysql.persistence.size=20Gi \
  --set ingress.enabled=true

# 3. 使用自定义values文件
helm install aims helm/aims -n aims --create-namespace -f custom-values.yaml
```

### 配置管理

#### 环境变量配置

编辑 `k8s/secret.yaml` 或 `helm/aims/values.yaml`：

```yaml
# LLM API Keys
DEEPSEEK_API_KEY: "your-deepseek-api-key"
MOONSHOT_API_KEY: "your-moonshot-api-key"
ZHIPU_API_KEY: "your-zhipu-api-key"

# 渠道配置
FEISHU_BOT1_APP_ID: "your-bot-app-id"
FEISHU_BOT1_APP_SECRET: "your-bot-app-secret"
WEWORK_CORP_ID: "your-corp-id"
WEWORK_AGENT_SECRET: "your-agent-secret"
```

#### 资源配置

```yaml
openclaw:
  replicaCount: 3
  resources:
    requests:
      memory: "512Mi"
      cpu: "500m"
    limits:
      memory: "2Gi"
      cpu: "2000m"
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80
```

### 验证部署

```bash
# 检查Pod状态
kubectl get pods -n aims

# 检查服务状态
kubectl get svc -n aims

# 检查Ingress状态
kubectl get ingress -n aims

# 查看日志
kubectl logs -f deployment/aims-openclaw -n aims
```

### 服务访问

配置本地hosts文件：

```bash
# 添加到 /etc/hosts (Linux/Mac) 或 C:\Windows\System32\drivers\etc\hosts (Windows)
<ingress-ip> aims.openclaw.local
<ingress-ip> aims-minio.openclaw.local
<ingress-ip> aims-milvus.openclaw.local
<ingress-ip> aims-qdrant.openclaw.local
```

访问服务：

- **OpenClaw Gateway**: http://aims.openclaw.local
- **MinIO Console**: http://aims-minio.openclaw.local
- **Milvus Dashboard**: http://aims-milvus.openclaw.local
- **Qdrant Dashboard**: http://aims-qdrant.openclaw.local

### 扩展和升级

```bash
# 水平扩展
kubectl scale deployment aims-openclaw --replicas=3 -n aims

# 使用Helm扩展
helm upgrade aims helm/aims -n aims --set openclaw.replicaCount=3

# 启用自动扩展
helm upgrade aims helm/aims -n aims \
  --set openclaw.autoscaling.enabled=true \
  --set openclaw.autoscaling.minReplicas=2 \
  --set openclaw.autoscaling.maxReplicas=5

# 更新配置
helm upgrade aims helm/aims -n aims -f custom-values.yaml
```

### 监控和日志

```bash
# 查看资源使用
kubectl top pods -n aims
kubectl top nodes

# 查看事件
kubectl get events -n aims --sort-by='.lastTimestamp'

# 查看日志
kubectl logs -f deployment/aims-openclaw -n aims
```

### 备份和恢复

```bash
# 备份MySQL
kubectl exec -it deployment/aims-mysql -n aims -- mysqldump -u root -p aims > backup.sql

# 恢复MySQL
kubectl exec -i deployment/aims-mysql -n aims -- mysql -u root -p aims < backup.sql

# 备份Milvus数据
kubectl exec -it deployment/aims-milvus -n aims -- tar -czf /tmp/milvus-backup.tar.gz /var/lib/milvus
kubectl cp aims/aims-milvus-xxx:/tmp/milvus-backup.tar.gz ./milvus-backup.tar.gz
```

### 清理部署

```bash
# 使用kubectl
kubectl delete namespace aims

# 使用Helm
helm uninstall aims -n aims
kubectl delete namespace aims
```

### 详细文档

详细K8s部署配置请参考：[k8s/部署指南.md](./k8s/部署指南.md)

---

## 四、环境对比和选择

### 功能对比

| 功能 | 本地开发 | Docker Compose | Kubernetes |
|------|----------|----------------|------------|
| 快速启动 | ✅ | ✅ | ❌ |
| 资源隔离 | ❌ | ✅ | ✅ |
| 水平扩展 | ❌ | ❌ | ✅ |
| 自动扩缩容 | ❌ | ❌ | ✅ |
| 滚动更新 | ❌ | ❌ | ✅ |
| 高可用 | ❌ | ❌ | ✅ |
| 监控告警 | ❌ | ❌ | ✅ |
| 成本 | 低 | 中 | 高 |

### 适用场景

- **本地开发环境**：日常开发、功能调试、单元测试
- **Docker Compose测试环境**：集成测试、功能验证、演示环境
- **Kubernetes生产环境**：生产运行、高可用、大规模部署

---

## 五、部署最佳实践

### 1. 安全建议

- **修改默认密码**：部署前修改所有默认密码
- **启用TLS**：生产环境启用HTTPS
- **网络策略**：配置适当的网络策略
- **RBAC**：配置适当的角色和权限
- **密钥管理**：使用Kubernetes Secrets或外部密钥管理系统

### 2. 性能优化

- **资源配置**：根据实际负载调整CPU和内存限制
- **存储优化**：使用高性能存储类
- **网络优化**：配置适当的网络策略和Ingress
- **缓存策略**：合理配置Redis缓存

### 3. 监控告警

- **指标监控**：集成Prometheus和Grafana
- **日志收集**：配置ELK或Loki日志收集
- **告警配置**：配置关键指标告警

### 4. 备份策略

- **定期备份**：配置定期自动备份
- **异地备份**：实现异地容灾
- **备份验证**：定期验证备份可用性

### 5. 升级策略

- **滚动更新**：使用滚动更新避免服务中断
- **金丝雀发布**：新版本先小范围验证
- **回滚机制**：配置快速回滚机制

---

## 六、故障排查

### 通用排查步骤

1. **检查服务状态**
   ```bash
   # 本地环境
   docker compose ps
   
   # K8s环境
   kubectl get pods -n aims
   ```

2. **查看日志**
   ```bash
   # 本地环境
   docker compose logs -f [service-name]
   
   # K8s环境
   kubectl logs -f deployment/[deployment-name] -n aims
   ```

3. **检查配置**
   ```bash
   # 检查环境变量
   docker compose config
   kubectl get configmap -n aims
   kubectl get secret -n aims
   ```

4. **检查网络**
   ```bash
   # 检查端口
   netstat -ano | findstr :18789
   
   # 检查连接
   telnet localhost 18789
   ```

### 常见问题

#### 问题1：服务无法启动

**症状**：Pod/容器一直处于Pending或CrashLoopBackOff状态

**排查**：
```bash
# 查看Pod详情
kubectl describe pod <pod-name> -n aims

# 查看事件
kubectl get events -n aims --sort-by='.lastTimestamp'
```

**解决方案**：
- 检查资源是否充足
- 检查镜像是否可拉取
- 检查配置是否正确

#### 问题2：服务无法访问

**症状**：无法通过Ingress或端口访问服务

**排查**：
```bash
# 检查Service
kubectl describe svc <service-name> -n aims

# 检查Ingress
kubectl describe ingress <ingress-name> -n aims
```

**解决方案**：
- 检查Service配置
- 检查Ingress配置
- 检查网络策略

#### 问题3：数据库连接失败

**症状**：应用无法连接到数据库

**排查**：
```bash
# 测试数据库连接
kubectl exec -it deployment/aims-openclaw -n aims -- mysql -h aims-mysql -u aims -paims@2026
```

**解决方案**：
- 检查数据库服务状态
- 检查连接配置
- 检查网络策略

---

## 七、支持和帮助

### 文档资源

- [本地开发环境部署指南.md](./本地开发环境部署指南.md) - 本地开发环境详细配置
- [k8s/部署指南.md](./k8s/部署指南.md) - K8s部署详细配置
- [快速启动指南.md](./快速启动指南.md) - 快速启动指南
- [更新日志.md](./更新日志.md) - 项目更新日志

### 问题反馈

如有问题，请通过以下方式反馈：
- 提交Issue到项目仓库
- 联系技术支持团队
- 参与社区讨论

---

*所有路径已锁定D盘，C盘零写入*