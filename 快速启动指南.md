# AIMS快速启动指南

> 更新日期：2026-04-28
> 目标：5分钟内启动AIMS系统

---

## 前置条件

- ✅ Docker Desktop已安装并运行
- ✅ OpenClaw已全局安装（`npm install -g openclaw`）
- ✅ 项目已克隆到 `D:\Project\aims\`
- ✅ `.env` 文件已配置（复制 `.env.example` 并填写API密钥）

---

## 一键启动

### 方式1：使用启动脚本（推荐）

```bash
# 双击运行或在命令行执行
D:\Project\aims\start-aims.bat

# 选择选项1：启动OpenClaw服务（开发模式）
```

### 方式2：使用PowerShell脚本

```bash
powershell -ExecutionPolicy Bypass -File "D:\Project\aims\start-aims.ps1" -Choice 1
```

### 方式3：快速启动脚本

```bash
D:\Project\aims\launch-openclaw.bat
```

---

## 验证启动

### 1. 检查Gateway状态

```bash
# 方式1：浏览器访问
# 打开浏览器访问：http://localhost:18789

# 方式2：命令行检查
powershell -ExecutionPolicy Bypass -Command "Invoke-WebRequest http://localhost:18789/health -UseBasicParsing | Select-Object StatusCode"
# 应返回：StatusCode: 200
```

### 2. 检查服务日志

```bash
# 查看OpenClaw日志
openclaw logs

# 或查看日志文件
type "C:\Users\...\AppData\Local\Temp\openclaw\openclaw-2026-04-28.log"
```

### 3. 测试Agent响应

```bash
# 通过Gateway发送测试消息
openclaw message send --channel feishu --target "测试群" --message "你好"
```

---

## 启动Docker依赖（可选）

如果需要使用MySQL、Redis等中间件服务：

```bash
# 启动所有Docker服务
docker compose up -d

# 查看服务状态
docker compose ps

# 停止服务
docker compose down
```

---

## 常用命令

### OpenClaw命令

```bash
# 查看版本
openclaw --version

# 查看帮助
openclaw --help

# 查看Gateway状态
openclaw status

# 查看已安装的Skills
openclaw skills list

# 查看通道状态
openclaw channels status

# 查看Agent状态
openclaw agents status

# 查看健康检查
openclaw doctor
```

### 环境验证

```bash
# 运行完整环境测试
powershell -ExecutionPolicy Bypass -File "D:\Project\aims\test-openclaw.ps1"
```

---

## 故障排查

### 问题1：端口被占用

**症状**：启动失败，提示端口18789已被占用

**解决方案**：

```bash
# 查找占用端口的进程
netstat -ano | findstr :18789

# 终止进程（替换<PID>为实际进程ID）
taskkill /PID <PID> /F
```

### 问题2：配置文件未找到

**症状**：提示"Missing config"

**解决方案**：

```bash
# 检查配置文件是否存在
dir D:\Project\aims\openclaw.json

# 设置环境变量
set OPENCLAW_CONFIG_PATH=D:\Project\aims\openclaw.json
```

### 问题3：API密钥无效

**症状**：LLM调用失败

**解决方案**：

1. 检查 `.env` 文件中的API密钥是否正确
2. 检查网络连接
3. 查看OpenClaw日志获取详细错误信息

### 问题4：内存不足

**症状**：JavaScript heap out of memory

**解决方案**：

```bash
# 增加Node.js内存限制
set NODE_OPTIONS=--max-old-space-size=4096

# 或使用Docker部署（资源管理更优）
docker compose up -d
```

---

## 配置说明

### 核心配置文件

| 文件 | 说明 | 位置 |
|------|------|------|
| openclaw.json | OpenClaw主配置 | D:\Project\aims\openclaw.json |
| mcporter.json | MCP Server注册表 | D:\Project\aims\mcporter.json |
| SOUL.md | 主Agent人设 | D:\Project\aims\SOUL.md |
| AGENTS.md | Agent定义文档 | D:\Project\aims\AGENTS.md |
| .env | 环境变量 | D:\Project\aims\.env |

### 关键环境变量

```env
# Gateway配置
AIMS_GATEWAY_TOKEN=aims-secret-token-2026
AIMS_GATEWAY_PORT=18789

# LLM API密钥
DEEPSEEK_API_KEY=your-deepseek-api-key
MOONSHOT_API_KEY=your-moonshot-api-key
ZHIPU_API_KEY=your-zhipu-api-key

# 通道凭据
FEISHU_BOT1_APP_ID=your-app-id
FEISHU_BOT1_APP_SECRET=your-app-secret
# ... 其他通道配置
```

---

## 下一步

启动成功后，可以：

1. **配置通道**：连接飞书、企微等消息通道
2. **测试Agent**：发送测试消息验证Agent响应
3. **开发Skill**：创建自定义业务技能
4. **集成MCP**：接入外部API和数据源

详细开发指南请参考：
- [本地开发环境部署指南.md](./本地开发环境部署指南.md)
- [AGENTS.md](./AGENTS.md)
- [2026041501任务分解清单.md](./2026041501任务分解清单.md)

---

## 获取帮助

- **OpenClaw文档**：https://docs.openclaw.ai
- **项目状态**：[PROJECT_STATUS.md](./PROJECT_STATUS.md)
- **问题反馈**：提交Issue到项目仓库

---

*所有路径已锁定D盘，C盘零写入*