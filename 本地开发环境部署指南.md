# AIMS本地开发环境部署指南

> 更新日期：2026-04-28
> 适用环境：Windows 10/11

---

## 环境要求

### 必需软件
- **Docker Desktop**：4.0+ 版本（用于运行中间件服务）
- **Git**：最新版本（代码管理）
- **Node.js**：18+ 版本（OpenClaw运行时）
- **Python**：3.10+ 版本（可选，用于自定义MCP Server）

### 推荐配置
- **内存**：8GB+（建议16GB）
- **磁盘**：D盘至少50GB可用空间
- **网络**：稳定的互联网连接（用于访问LLM API）

---

## 目录结构

所有文件必须锁定在 `D:\Project\aims\` 目录下：

```
D:\Project\aims\
├── openclaw.json              # OpenClaw主配置
├── mcporter.json              # MCP Server注册表
├── SOUL.md                   # 主Agent人设
├── AGENTS.md                 # Agent定义文档
├── .env                      # 环境变量（不提交到Git）
├── .env.example              # 环境变量模板
├── docker-compose.yml         # Docker编排文件
├── start-aims.bat            # Windows启动脚本
├── start-aims.ps1            # PowerShell启动脚本
├── launch-openclaw.bat        # 快速启动脚本
├── test-openclaw.ps1         # 环境测试脚本
├── workspace-main/           # main Agent工作区
├── workspace-ecommerce/      # 电商Agent工作区
├── workspace-social/         # 社媒Agent工作区
├── workspace-cs/            # 客服Agent工作区
├── workspace-office/         # 办公Agent工作区
├── skills/                  # 自定义Skills目录
├── data/                   # 数据存储目录
│   ├── mysql-data/
│   ├── redis-data/
│   ├── milvus-data/
│   ├── qdrant-data/
│   └── minio-data/
├── logs/                   # 日志目录
└── env/                    # 本地依赖隔离
    ├── venv/               # Python虚拟环境
    ├── node-store/          # Node.js缓存
    └── pip-cache/          # pip缓存
```

---

## 安装步骤

### 1. 克隆项目

```bash
git clone <repository-url> D:\Project\aims
cd D:\Project\aims
```

### 2. 安装OpenClaw

```bash
npm install -g openclaw
```

验证安装：

```bash
openclaw --version
# 应显示：OpenClaw 2026.3.13 (61d171a)
```

### 3. 配置环境变量

复制环境变量模板：

```bash
copy .env.example .env
```

编辑 `.env` 文件，配置以下关键参数：

```env
# Gateway配置
AIMS_GATEWAY_TOKEN=aims-secret-token-2026
AIMS_GATEWAY_PORT=18789

# LLM API密钥
DEEPSEEK_API_KEY=your-deepseek-api-key
MOONSHOT_API_KEY=your-moonshot-api-key
ZHIPU_API_KEY=your-zhipu-api-key

# 通道凭据（根据需要配置）
FEISHU_BOT1_APP_ID=your-app-id
FEISHU_BOT1_APP_SECRET=your-app-secret
# ... 其他通道配置
```

### 4. 创建Python虚拟环境（可选）

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

### 5. 创建必要目录

```powershell
@("D:\Project\aims\data\mysql", "D:\Project\aims\data\redis", "D:\Project\aims\data\milvus", "D:\Project\aims\data\qdrant", "D:\Project\aims\data\minio") | ForEach-Object {
    if (-not (Test-Path $_)) {
        New-Item -ItemType Directory -Path $_ | Out-Null
    }
}
```

---

## 启动方式

### 方式1：交互式启动（推荐）

双击运行 `start-aims.bat`，选择启动模式：

1. **启动OpenClaw服务（开发模式）** - 启动OpenClaw Gateway
2. **启动OpenClaw服务（调试模式）** - 启动OpenClaw Gateway（详细日志）
3. **启动Docker依赖服务（测试环境）** - 启动MySQL、Redis等中间件
4. **验证环境配置** - 检查所有配置文件和目录
5. **退出** - 退出脚本

### 方式2：命令行启动

```bash
# 启动OpenClaw服务
powershell -ExecutionPolicy Bypass -File "D:\Project\aims\start-aims.ps1" -Choice 1

# 启动Docker依赖
docker compose up -d

# 快速启动OpenClaw
D:\Project\aims\launch-openclaw.bat
```

### 方式3：直接使用OpenClaw命令

```bash
# 设置环境变量
set NODE_OPTIONS=--max-old-space-size=4096
set OPENCLAW_CONFIG_PATH=D:\Project\aims\openclaw.json

# 启动Gateway
openclaw gateway --port 18789 --allow-unconfigured
```

---

## 验证安装

### 1. 环境验证

```bash
powershell -ExecutionPolicy Bypass -File "D:\Project\aims\test-openclaw.ps1"
```

预期输出：

```
[Test 1] Checking OpenClaw installation...
OK: OpenClaw found at C:\Users\...\openclaw.cmd
Version: OpenClaw 2026.3.13 (61d171a)

[Test 2] Checking configuration file...
OK: Configuration file found

[Test 3] Validating configuration syntax...
OK: Configuration JSON is valid
  - Agents defined: 5
  - Models configured: deepseek, moonshot, zhipu
  - Channels enabled: feishu, wework, dingtalk, telegram, whatsapp, discord

[Test 4] Checking workspace directories...
OK: workspace-main
OK: workspace-ecommerce
OK: workspace-social
OK: workspace-cs
OK: workspace-office

[Test 5] Checking environment file...
OK: .env file exists

All tests completed successfully!
```

### 2. Gateway健康检查

```bash
powershell -ExecutionPolicy Bypass -Command "Invoke-WebRequest http://localhost:18789/health -UseBasicParsing | Select-Object StatusCode"
```

预期输出：`StatusCode: 200`

### 3. Docker服务检查

```bash
docker compose ps
```

预期输出：所有服务状态为 `Up`

---

## 常见问题

### Q1: OpenClaw启动失败，提示"Missing config"

**原因**：未正确设置 `OPENCLAW_CONFIG_PATH` 环境变量

**解决方案**：
```bash
set OPENCLAW_CONFIG_PATH=D:\Project\aims\openclaw.json
openclaw gateway --port 18789 --allow-unconfigured
```

### Q2: JavaScript heap out of memory

**原因**：Node.js默认内存限制不足

**解决方案**：
```bash
set NODE_OPTIONS=--max-old-space-size=4096
```

### Q3: Docker服务启动失败

**原因**：端口被占用或Docker未运行

**解决方案**：
1. 检查Docker Desktop是否运行
2. 检查端口是否被占用：`netstat -ano | findstr :18789`
3. 查看Docker日志：`docker compose logs`

### Q4: 通道消息无法接收

**原因**：通道凭据配置错误或网络问题

**解决方案**：
1. 检查 `.env` 文件中的API密钥是否正确
2. 检查网络连接
3. 查看OpenClaw日志：`openclaw logs`

---

## 开发工作流

### 1. 启动开发环境

```bash
# 启动Docker中间件
docker compose up -d

# 启动OpenClaw Gateway
D:\Project\aims\start-aims.bat
# 选择选项1
```

### 2. 开发自定义Skill

```bash
cd skills
mkdir my-skill
cd my-skill

# 创建SKILL.md
# 创建index.js或index.py

# 测试Skill
openclaw skills install --local ./my-skill
```

### 3. 开发MCP Server

```bash
# 激活Python虚拟环境
.venv\Scripts\activate

# 创建MCP Server
mkdir mcp-servers\my-mcp
cd mcp-servers\my-mcp

# 编写MCP Server代码
# 创建server.py

# 注册MCP Server
# 编辑mcporter.json
```

### 4. 查看日志

```bash
# OpenClaw日志
openclaw logs

# Docker服务日志
docker compose logs -f [service-name]
```

---

## 路径约束（重要）

所有路径必须锁定D盘，C盘零写入：

- ✅ 工作目录：`D:\Project\aims\workspace-*`
- ✅ 数据存储：`D:\Project\aims\data\*`
- ✅ 日志文件：`D:\Project\aims\logs\*`
- ✅ 依赖缓存：`D:\Project\aims\env\*`
- ✅ 配置文件：`D:\Project\aims\*.json`, `D:\Project\aims\.env`

---

## 下一步

完成本地环境搭建后，可以开始：

1. **P1阶段**：实现电商核心功能
2. **P2阶段**：实现社媒核心功能
3. **P3阶段**：实现高级功能

详细任务清单请参考 [2026041501任务分解清单.md](./2026041501任务分解清单.md)

---

*所有路径已锁定D盘，C盘零写入*